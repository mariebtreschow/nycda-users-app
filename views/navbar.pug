nav.navbar.navbar-inverse
  .container-fluid

    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      ul.nav.navbar-nav
        li
          a(href='/') USER INFORMATION
        li
          a(href='/search') SEARCH USER
        li
          a(href='/add-user') ADD USER
        form(action="/search-navbar" method="GET").navbar-form.navbar-left
          .form-group
            input#searchFor.form-control(name="searchFor" type='text', placeholder='Search')
            button.btn.btn-default(type='submit') Submit
        li
          button(class="btn-lg btn-success like-button pull-right") LIKE THE APP
